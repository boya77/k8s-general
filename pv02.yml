apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv02
spec: 
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Recycle
  nfs:
    path: /srv/demo2
    server: master
